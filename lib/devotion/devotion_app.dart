import 'package:flutter/material.dart';

class DevotionApp extends StatelessWidget {
  const DevotionApp({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Column()
      
    );
  }
}